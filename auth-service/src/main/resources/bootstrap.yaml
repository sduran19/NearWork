spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://localhost:9000
      username: admin
      password: admin
      enabled: true
      profile: dev
jwt:
  secret: secret

